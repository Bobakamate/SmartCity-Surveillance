{
  "framework": "vite",
  "installCommand": "npm install --no-optional --no-package-lock --no-audit",
  "buildCommand": "npx -p rollup -c 'npm run build'",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
